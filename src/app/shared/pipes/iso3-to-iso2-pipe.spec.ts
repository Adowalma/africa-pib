import { Iso3ToIso2Pipe } from './iso3-to-iso2-pipe';

describe('Iso3ToIso2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Iso3ToIso2Pipe();
    expect(pipe).toBeTruthy();
  });
});
